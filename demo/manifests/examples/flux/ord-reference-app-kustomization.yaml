apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ord-reference-app
  namespace: default
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: local-event-showcase
  path: ./demo/manifests/examples/kustomize/ord-reference-app
  prune: true
  kubeConfig:
    secretRef:
      name: my-cluster-kubeconfig
      key: kubeconfig
